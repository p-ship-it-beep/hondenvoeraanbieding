# Huisdierdeal.nl

## Deployment

This repo is connected to GitHub (`p-ship-it-beep/hondenvoeraanbieding`, branch `main`), which is linked to Netlify for auto-deploy on push.

- After making changes, always commit and push to `main` automatically — do not ask for confirmation before pushing. The user has explicitly authorized this.
- Write clear, concise commit messages describing the change.
